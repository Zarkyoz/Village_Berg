{% extends 'base.html.twig'%}

{% block title %}Actualités de Berg{% endblock %}

{% block body %}
    <h1>Actualités</h1>
    
        <section class="partieArticle">
    
            {% for articleActu in articleActus %}
                <article class="boucleArticle" >
                    <img src="{{ articleActu.imageActu }}" width="250" alt="">
                    <section>
                    <h2> {{ articleActu.titleActu }} </h2>
                         <article>
                            {{articleActu.createdAdActu | date('d-m-Y H:i:s', "Europe/Paris") }}
                        </article>
                    <p> {{ articleActu.descriptionActu|slice(0,70)}}<a href="{{ path('actu_Show', {'id': articleActu.id}) }}">...</a> </p>
                    <a href="{{ path('actu_Show', {'id': articleActu.id}) }} "><button>Lire la suite</button></a>
                    </section>
                </article>
            {% endfor %}
        </section>


{% endblock %}