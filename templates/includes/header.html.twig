<header>
	<section>
		<article id="logoTitre">
			<img src="{{ asset('assets/images/logoBerg.png')}}" alt=""></article>
		<article>
			<ul>
				<li><a href="{{ path ('home') }}">Accueil</a></li>
			</ul>
		</article>
	</section>
	<section>
		<article>
			<ul>
				<li>
					<a href="{{ path ('histoire') }} ">Histoire</a>
				</li>
				<li>
					<a href="{{ path ('evenements') }} ">Evénements</a>
				</li>
				<li>
					<a href="{{ path ('actualites') }} ">Actualités</a>
				</li>
				<li>
					<a href="{{ path ('contact') }}">Contact</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
				<li>
					<a href="{{path ('admin_village_index') }} ">Admin</a>
				</li>
				{% endif %}
				{# <li><a href="{{ path ('redi') }}">redircetion vers l'ailleurs</a></li> #}
			</ul>
		</article>
	</section>
    <section>
        <article>
            <ul>
                {% if not app.user %}
                    <li><a href="{{path ('app_login') }}">Connexion</a></li>
                {% else %}
                    <li><a href="{{path ('app_logout') }}">Deconnexion</a></li>
                {% endif %}
                
            </ul>
        </article>
    </section>
</header>
